import React, { Fragment } from "react";
import BackToTop from "react-custom-back-to-top-button";

const BacktoTop = () => {
  return (
    <Fragment>
      <BackToTop className="shadow" style={{ backgroundColor: "#f0401b" }} />
    </Fragment>
  );
};

export default BacktoTop;
